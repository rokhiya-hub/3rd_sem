# Ask the user to enter a string
string = input("Enter a string: ")

# Convert the string into a list of individual characters
# Each character (including spaces or punctuation) becomes one element in the list
char_list = list(string)

# Display the resulting list of characters
print("Array of characters:", char_list)