# Loop through numbers starting from 8 up to (but not including) 90,
# increasing by 3 each time.
for num in range(8, 90, 3):
    # Print the current number on the same line, separated by spaces.
    # 'end=" "' prevents printing on a new line after each number.
    print(num, end=' ')